# 運用健康診断（2026-07-07 / Fable 5 作成）

運用中プロダクトとしての健康状態のみを診る（新機能提案はしない）。状況が変わっていたら premise-check してから従うこと。

## 結論

**本番は健全**。フロント(trip-road.tetutetu214.com 200)・データ配信(municipalities/adjacency/conquest_meta/regions すべて200)・Workers API(未認証401=正常)を実測確認。テストはフロント172件+Workers 211件=**383件全pass**。緊急対応は不要。最終コミットは 2026-06-10 で約4週間の休止中。

## 所見（リスク順）

1. **[高・変更時のみ] データ配信はクロスプロジェクト契約**: fog-walker が `municipalities/{5桁}.geojson` と `conquest_meta.json` を直接 fetch している（fog-walker src/config.ts:138-139 で確認済み）。**URL構造・フィールド名・CORS(`*`) を変える変更は fog-walker を静かに壊す**。配信に触れる PR では fog-walker への影響確認をレビュー項目に必ず入れる。
2. **[中] IAM 長期キーのローテーションが手動トリガ**: rotate_aws_key.sh は存在するが定期実行の仕組みがない。最終ローテーション 2026-06-10。月次棚卸し（inventory.py）のタイミングで再実行する運用にするのが現実的。
3. **[低] E2E の既知失敗が未解決のまま**: 直近既知結果 16 passed / 2 failed（Issue #73 visibilitychange の地図サイズ）。再開時の最初の修正候補。
4. **[低] 古い feature ブランチ3本が main から80〜104コミット遅れで残存**: elevation-and-hillshade / hillshade-three-levels / nova-migration。機能は main に取込済みの残骸に見えるが、**中身の確認をしてから削除**（unmerged 表示のため機械的に消さない）。
5. **[低] docs ドリフト**: wrangler.toml コメントが旧 ANTHROPIC_API_KEY 前提のまま / todo.md:312 の副次タスクは実装済みなのに未チェック。
6. **[監視なし・許容] Mapbox 無料枠(50k ロード/月)・Bedrock/DynamoDB 従量課金は監視なし**: 入口は APP_PASSWORD で守られており現状リスク低。気になったら whatsnew-agent と同じ死活アラートのパターンを移植できる。

## 次に再開するときの順番

1. Issue #73（E2E 2件失敗）の修正 → E2E 全green化
2. ブランチ3本の中身確認 → 削除
3. docs ドリフト解消（chore）
4. その後に Issue リスト（#75 app.js 責務分解 ほか）へ
