# 監査 2026-07-18（Fable 最終監査・audit-20260712 の更新版）

Fable サブスク枠の最終日に実施した更新監査。以降は Opus/Sonnet + `/fable` skill 運用。

## 所見1（中→解決）: Issue #73 E2E test#4 失敗 — 根因確定・修正 PR #78 作成済み

**根因（1文）**: test#4 だけが「土地のたより」生成パイプラインの完了を待たずに固定 2000ms 待機で地図高さの before/after を計測するため、生成フェーズ遷移（skeleton→生成中→判定中→確定）で `.bottom-card` の高さが変わり、ResizeObserver が `--card-height` 経由で `#map` の高さを動かし、visibilitychange と無関係に高さ差が出る。

**証拠**:
- 失敗時実測: mapBefore.height=479px → mapAfter.height=381px（差 98px、許容 <5px）
- 失敗時スクリーンショットが「✓ 内容を確認しています…」（judging フェーズ途中）を捉えていた
- 同一条件のリトライで pass（flaky）。Mapbox GL JS v3 の resize 実バグ説（仮説B）を支持する証拠なし
- 修正（test#5 と同じ muni-name 確定→skeleton 非表示の待機を追加）後、`--repeat-each=3 --retries=0` で **3/3 pass**

**状態**: PR #78（`fix/issue73-e2e-wait-description`）レビュー待ち。マージで Issue #73 クローズ。

## 所見2（高→対応済み）: E2E 調査中に APP_PASSWORD がセッションログへ露出 → 即時ローテーション実施

調査サブエージェントが bash 変数展開の誤用で APP_PASSWORD の実値を会話ログ（Anthropic へ送信される）に露出させた。CLAUDE.md の漏洩時プロトコルに従い同セッション内で対応済み:

1. 影響評価: アプリの閲覧パスワード（Workers Secret）のみ。クラウド認証情報・長期キーではない
2. ローテーション: 新値（32桁hex）を `wrangler secret put APP_PASSWORD` で適用（値は非表示のままパイプ）。適用証拠 = Worker version `21d382aa`（2026-07-17T22:34 UTC、Source: Secret Change）
3. `~/.secrets/trip-road.env` は新値を末尾追記（source 時は後勝ちで新値有効）。**旧 APP_PASSWORD 行が残っているので、任意のタイミングで手動削除推奨**
4. 動作確認: 新パスワードで E2E ログイン成功（所見1の検証 3/3 pass がそのまま証拠）
5. 再発防止: サブエージェントへの委譲時のシークレット取り扱いルールをメモリに追記

**てつてつへの影響**: iPhone 等でパスワードを保存していた場合、次回アクセスで再入力が必要。新値は `~/.secrets/trip-road.env` の末尾行。

## 所見3（低）: feature ブランチ残骸 — 判定済み・実行のみ残

diff 0 の3本（`feature/elevation-and-hillshade` `feature/hillshade-three-levels` `feature/nova-migration`）は削除可の判定を再確認済み。origin-only 3本（generator-prompt-tuning / map-initial-view-and-night-track / mapbox-main-map）は未突合のまま。

## 所見4（低）: docs ドリフト — 未着手のまま有効

`workers/wrangler.toml:11` の旧 ANTHROPIC_API_KEY コメント残存、`docs/todo.md:317` の完了済み項目が未チェックのまま。

## 新規所見（2026-07-18）

- **所見5（低）: 未追跡ファイル2件**。`AGENTS.md`（symlink、07-14 作成、Codex 委譲用）と `workers/.claude/`（settings.local.json、05-06 付）。前者は chore コミット、後者は `.gitignore` 追加が候補。

## 次の一手（優先順）

1. ⏳てつてつ: PR #78 のレビュー → マージ（Issue #73 クローズ）
2. ⏳てつてつ: `~/.secrets/trip-road.env` の旧 APP_PASSWORD 行（コメントより上の行）を手動削除 + スマホ保存パスワードの更新
3. diff 0 ブランチ3本の削除実行 + origin-only 3本の突合
4. docs ドリフト解消（wrangler.toml コメント・todo.md チェック）+ AGENTS.md / workers/.claude/ の扱い確定
